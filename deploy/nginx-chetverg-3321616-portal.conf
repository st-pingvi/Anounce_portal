server {
    listen 80;
    listen [::]:80;
    server_name chetverg.3321616.ru;

    root /var/www/gos-portal;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
